# Copy to .env and fill in. .env is gitignored and must never be committed.
# Owner connection string from the Neon dashboard. Used only by setup and ingestion.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require
# The two below are generated by ingest/m0_setup.py. Do not fill in by hand.
# INGEST_DATABASE_URL=
# APP_DATABASE_URL=
